import {expect} from "chai";

// Place any unit tests in files with the same name as code under test with the .spec.ts extension.
// Delete this file if unit tests of index.ts are not needed.
describe("index", () => {
	it("does the thing", () => {
		expect(true).to.be.true;
	});
});
