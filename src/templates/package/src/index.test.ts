import {expect} from "chai";

// Place any integration tests in files with the same name as code under test with the .test.ts
// extension. Delete this file if integration tests of index.ts are not needed.
describe("index", () => {
	it("does the thing", () => {
		expect(true).to.be.true;
	});
});
